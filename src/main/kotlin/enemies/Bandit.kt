package enemies

class Bandit(name: String,
             maxHealth: Int,
             currentHealth: Int,
             initiative: Int): Enemy(name,
                                    maxHealth,
                                    currentHealth,
                                    initiative) {
}