package org.example.Enemies

open class Summoner(name: String,
                    maxHealth: Int,
                    currentHealth: Int,
                    initiative: Int): Enemy(name,
                                            maxHealth,
                                            currentHealth,
                                            initiative){
}