package org.example.Enemies

class Kobold(name: String,
             maxHealth: Int,
             currentHealth: Int,
             initiative: Int): Enemy(name,
                                    maxHealth,
                                    currentHealth,
                                    initiative){
}