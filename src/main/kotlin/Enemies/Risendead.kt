package org.example.Enemies

class Risendead(name: String,
                maxHealth: Int,
                currentHealth: Int,
                initiative: Int): Summoner(name,
                                            maxHealth,
                                            currentHealth,
                                            initiative) {
}