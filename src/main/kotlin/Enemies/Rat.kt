package org.example.Enemies

class Rat(name: String,
          maxHealth: Int,
          currentHealth: Int,
          initiative: Int): Enemy(name,
                                    maxHealth,
                                    currentHealth,
                                    initiative){
}